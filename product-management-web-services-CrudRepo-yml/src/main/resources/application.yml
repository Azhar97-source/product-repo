spring:
  jpa:   show-sql: true
   hibernate:
    ddl-auto: create
  datasource:
    url: jdbc:mysql://localhost:3306/javafs
    username: root
    password: root  
    
server:
 servlet:
  context-path: /myApp        